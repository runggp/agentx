secrets.env
.env
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.venv/
dist/
*.egg-info/

# Ephemeral task focus — written by listener or `ralph.sh task`
TASK.md

# Host-specific runtime config — managed on VPS, not in repo
litellm-config.yaml

# Ralph runtime — never commit these
.agent-claude/
.agent-ssh/
scaffold/
.entire/
.claude/
